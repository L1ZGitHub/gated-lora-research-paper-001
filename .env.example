# Hugging Face Hub token (for model download + checkpoint upload)
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Wandb API key (optional, only if WANDB_MODE != offline)
# WANDB_API_KEY=

# Anthropic key (only for VPS supervisor — never on Ensimag)
# ANTHROPIC_API_KEY=
