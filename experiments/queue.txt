# Experiments to run (in order). Lines starting with # are comments.
# Format: <config_path> <seed1> [<seed2> ...]
#
# The supervisor reads this to:
#  - know what's "in flight" vs "idle" (avoids false stall warnings)
#  - suggest the next experiment to launch when the user is idle
#
# Empty / all-commented = idle (supervisor emits info only, no warnings).

# === Phase 0: smoke test (10-15 min, validates stage-in/stage-out pipeline) ===
configs/experiments/smoke_phi2.yaml 0

# === Phase 1: Phi-2 baseline (3 seeds — first real paper datapoint) ===
# configs/experiments/phi2_harder_multitask.yaml 42 1337 2024

# === Phase 2: Architecture sweep (1 seed each first, then expand) ===
# configs/experiments/gemma2_all_8.yaml 42
# configs/experiments/pythia410m_all_8.yaml 42

# === Phase 3: Multi-seed for chosen models (paper figures) ===
# configs/experiments/gemma2_all_8.yaml 1337 2024
# configs/experiments/pythia410m_all_8.yaml 1337 2024

# === Phase 4: Ablations on Phi-2 ===
# configs/experiments/phi2_ablation_no_l1.yaml 42 1337 2024
